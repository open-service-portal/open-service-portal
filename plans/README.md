# plans/

Multi-step implementation plans, one per feature (`<feature>.md`). A plan buys an
overview, the option to fan work out, and resumability across sessions.

Plans are **scaffolding**: drop the file once its tasks are done (the spec, tests,
code, docs, and journal carry the lasting record). Small single-step tasks don't
need a plan — don't manufacture one.
