# Claude settings
.claude/settings.local.json
.claude/*.local.*

# Cloned repositories (managed separately)
.github/
github-org-settings/
/app-portal/
catalog/
catalog-orders/
docusaurus/
ingestor/
oidc-authenticator/
backstage-plugins/

# Service Template Repositories
templates/
template-*/
service-*-template/

# Reference Repositories (for documentation search) — optional personal clones,
# kept wherever you like (workspace root or _work/reference/). Ignored, not enforced.
/backstage*/
crossplane*/

# K8s Configs
*.kubeconfig
*-kubeconfig
*kubeconfig.yaml

# Environment Variables
.env*
!.env*.example

# OS
.DS_Store
Thumbs.db

# Scripting
node_modules/

# IDE
.vscode/
.idea/
*.code-workspace

# MCP
.playwright-mcp/

# Temporary
*.swp
*~
debug/

# Local resources
.local/
*.local.*
.env.rancher-desktop

# Local-only Claude Code safety hooks (personal allowlist, not for the public repo)
.claude/hooks/

# real denylist = the internal names you hide; never commit it (only .example is shared)
.git-deny-patterns
